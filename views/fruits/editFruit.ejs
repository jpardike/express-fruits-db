<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Fruit</title>
</head>
<body>
  <h1>Edit Fruit</h1>

  <form action="/fruits/<%= fruit._id %>?_method=PUT" method="POST">
    <div>
      <label for="name">Name</label>
      <input type="text" id="name" name="name" value="<%= fruit.name %>" />
    </div>

    <div>
      <label for="color">Color</label>
      <input type="text" id="color" name="color" value="<%= fruit.color %>" />
    </div>

    <div>
      <label for="readyToEat">Ready To Eat?</label>
      <input type="checkbox" id="readyToEat" name="readyToEat" <%= fruit.readyToEat ? 'checked' : '' %> />
    </div>

    <!-- <button type="submit">Add Fruit</button> -->
    <input type="submit" value="Edit Fruit" />
  </form>
</body>
</html>